import React from 'react';
import Header from './components/Header';

const App = () => {
    return (
        <div>
            <Header />
            <main>
                <h1>Welcome to our app!</h1>
                <p>This is a sample app built with Tailwind CSS.</p>
            </main>
        </div>
    );
};

export default App;